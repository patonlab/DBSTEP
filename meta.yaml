{% set name = "dbstep" %}
{% set version = "1.2.0" %}
# NOTE: Update version above to match dbstep/__version__ on each release.
# The sha256 hash must also be updated from the PyPI sdist.

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 379e4c732e403c43f277ef06f899f64aa7fe5221b4092d3bca4185a109c2cd3c

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - pip
    - python >=3.9
    - setuptools >=64
  run:
    - cclib
    - numpy
    - python >=3.9
    - scipy

test:
  imports:
    - dbstep

about:
  home: "https://github.com/patonlab/DBSTEP"
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: "DFT-Based Steric Parameters"

extra:
  recipe-maintainers:
    - luchini18
